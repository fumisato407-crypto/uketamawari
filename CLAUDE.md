# 和菓子店 予約承り表タブレットアプリ

仕様の正本＝リポジトリ外の仕様書（uketamawari_spec.md v1）。ここは実装で確定した決定事項の引き継ぎ。

## 概要
- 店頭タブレット（**iPad・Safari**。仕様書のAndroid/Mopriaから変更）で予約入力→A4承り表を印刷
- サーバなし・素のHTML/JS/CSS・IndexedDB保存。ビルド工程なし、index.htmlを開くだけ
- 公開: **https://maruendo.github.io/uketamawari/** （GitHub Pages・mainにpushで自動反映）
  - 2026-08-30にOrganization「maruendo」へリポジトリを移動。
    個人アカウント名をURLに出したくないという要望のため。旧URL(fumisato407-crypto...)は無効
- push手段: ポータブルgh（%LOCALAPPDATA%\gh-portable\bin\gh.exe）で認証済み。通常のgit pushが通る

## 確定した運用・設計判断
- **操作分担**: 商品選び＝店主、お客様情報画面だけタブレットをお客に渡す。
  その画面の間は上部メニュー（予約一覧・設定）を非表示＝body.customer-mode（他客の個人情報保護）
- **印刷レイアウト**: A4横1枚に承り表を左右2面（各A5相当・中央に切り取り線）。
  **左=お客様控・右=店控で中身が違う**（下の「承り表の版」参照）。明細は7行。
  店の指示による配置なので勝手に変えない
- 価格null商品＝単価テンキー手入力。商品×入数=1SKU。注文時点の名前/価格をコピー保存
- **菓子・包材の大枠と明細の商品内容はプルダウン入力**（種類＋個数を選ぶ。実装済み）。
  紙の承り表への店主の手書き指示（reference/photos/IMG_3624.JPG）
  - 中身は**包材（箱・紙袋・風呂敷・クリアケース等）**で確定（2026-08-30）。
    プルダウン候補＝価格表の「箱」「紙袋」カテゴリ。菓子は入れない
  - 明細の「商品内容」＝詰合せの中身。候補は菓子（products.js の sweet:true）
  - 選択結果は picks 配列が正で、紙に出す文字列(note/packaging)はそこから組み立てる
  - **紙に出す形は「・最中×9」を1行ずつ縦に並べる（2026-09-01・店主指示）**。
    横に「・」で繋げると読みにくいとのこと。fmtPicks が改行区切りを返すので、
    出す側は white-space を効かせること（明細の商品内容=`td.c-note`はpre-line、
    菓子・包材の大枠=`.memo-box`はpre-wrap）。**1行ずつに戻さないこと**
  - 中身4件＋包材2件でもA4横1枚に収まることを確認済み（?demo=1 がこの重い例になっている）
### 承り表の版（2026-08-30実装済み・reference/photos/承り表新レイアウト_2026-08-30.png）
  この縦レイアウトを1面分として**A4横2面・切り取り線は維持で確定**（2026-08-30）。
  上部が左右2列（左=御名前/御住所/電話、右=御来店日時/配送/熨斗/表書き）・
  明細の空行は細罫線・下部左=合計/支払い/備考、下部右=大枠（見出し：トータル数 菓子・包材）
- **2面は同一内容ではなくなる（2026-08-30確定）**: 左=お客様用・右=店用。
  **お客様用には菓子・包材の大枠を印刷しない**（店用のみ）。写真=reference/photos/承り表2面指示_2026-08-30.png
- **お客様用の面はレシート風の新デザイン**（reference/承り表_お客様用_2026-08-30.docx）:
  上部2列（左=名前/住所/電話/表書き、右=来店日時/配送/熨斗/支払）→明細→
  「ご予約ありがとうございました 合計」→**店名・住所・TELを印字**
  （菓子舗丸円堂 〒871-0021 大分県中津市沖代町1丁目446番11 TEL 0979-64-6328）。
  備考・トータル数・菓子/包材枠なし。**店控は午前のWordレイアウトのまま**（店主確認済み）
- **支払い表示（2026-08-30・最終）**: Wordの様式どおり**「済・未」を並べて該当側に丸**
  （熨斗の内・外と同じ見せ方）。一時は「済＝赤字でお礼文」の案が出たが**取り下げ済み**。
  蒸し返さないこと
- **支払いの運用**: お客はアプリ入力後レジで会計→店主が承り表の入力を仕上げて渡す。
  つまり支払い済/未の入力は**店主側の画面（印刷前）**で行う（お客様画面には出さない）
- **商品選択のカテゴリ＝レジの5タブに合わせる（2026-08-30確定）**:
  詰合せ1 / 詰合せ2 / 定番 / 生菓子 / 箱 店内
- **予約一覧の状態表示（2026-09-01・店主指示）**: 「受付済／受渡済」は一字違いで紛らわしく、
  さらに受渡済と支払済が混同されるという指摘。画面では
  **お渡し＝まだ／済・お支払い＝まだ／済** と見出し付きで並べ、今どちら側かを常に表示する
  （保存データの status は "受付済"/"受渡済" のまま。表示だけ言い換えている）。
  **押した瞬間には切り替えず必ず確認を出す**（ワンタッチだとどちらだったか分からなくなるため）。
  一覧で支払いを変えたときは、同じ予約を開いていれば state 側も揃える
  （揃えないと印刷画面の「保存」で一覧の変更が巻き戻る）
- **印刷プレビューの下ボタンは来た経路で変える（2026-09-01・店主指示）**:
  予約一覧から開いた＝[予約一覧に戻る][編集する][保存][印刷] /
  入力の流れ＝[編集に戻る][保存][印刷]。判定は state.openedFrom
- **商品の編集はモーダルではなく独立した画面**（2026-09-01）。
  きっかけはキーボードの不具合調査だったが**それは誤診だった（下記）**。
  ただしタブレットでは全画面の方が押しやすく、実機で動作確認済みなのでこのまま。
  戻す理由がないので触らないこと

### 【重要】キーボードが出なかった件は **iPadOSが古かったのが原因**（2026-09-01・決着）
症状: 入力欄をタップするとフォーカスは当たる（カーソルが出る）のに、
ソフトキーボードだけが上がらない。アプリの全画面で発生。
**iPadOSをアップデートしたら直った。アプリのコードは無関係だった。**
- **同じ症状を見たら、まずiPadOSの更新有無を聞くこと。** コードを疑う前に。
  ここで私は v12→v14 まで3回、当て推量の修正を重ねて店主に無駄な検証をさせた
- 切り分けの型: **カーソルが出る＝タップもフォーカスも通っている**。
  CSSの覆いやタップ判定の問題ではない。そこでキーボードだけ出ないなら
  端末側（OSの不具合・物理キーボード接続）を先に疑う
- 実機で確定させる検査ページを作ってある。必要なら復活させる:
  `git show 5f9b1ef:kbtest.html > kbtest.html`
  （素の入力欄 / user-select:none付き / position:fixed内 / 本物のアプリ の4通りを試し、
   visualViewportの縮み方でキーボードが出たかを自動判定する。
   **検査1（素の入力欄）で出なければアプリは無関係**と即断できる）。
  原因が確定したので公開サイトからは消してある

### iPadのソフトキーボード周りで実際に直した本物のバグ
- **Enterでキーボードを閉じる処理は、日本語変換の確定Enterを必ず除外する**
  （`e.isComposing || e.keyCode === 229` で return）。除外しないと漢字に変換した瞬間に
  キーボードが消えて名前が打てない。**これは上のOS問題とは別の、実在したバグ**
- 入力欄の外をタップして閉じる処理は**即blurしない**。iOSは touchend → mousedown → focus の順なので、
  即blurすると「これから開くキーボード」を潰しうる。300ms待ってフォーカスが動いていないときだけ閉じる
- `user-select:none` は body ではなく「押すだけの部品」に限って掛けている。
  入力欄に効くとまずいという一般論からの予防であって、**今回の原因ではなかった**
- 動作確認は実機のみ。ヘッドレスでは `element.focus()` で focusin すら飛ばないことがある

### 版の届け方（「直したのに直っていない」の切り分け）
- **設定画面に版番号を出してある（APP_VERSION）**。まず店主に版番号を読んでもらう。
  **`APP_VERSION`（js/app.js）と `CACHE`（sw.js）は必ず同時に上げる**
- 設定画面の「アプリを最新にする」＝SW登録解除＋uketamawari-*キャッシュ削除＋再読込。
  店主が自分で最新版を取り直せる逃げ道（予約データはIndexedDBなので消えない）
- ホーム画面アプリは**1回閉じ直しただけでは新版にならない**（起動時に新SWが入り、
  次の起動でようやく新しいファイルが読まれる）。「2回閉じ直す」か上のボタンを案内する

- 画面の高さは **body を flex 縦積みにして .screen が余りを取る**。
  `calc(100% - 56px)` の決め打ちに戻すと、バックアップ促しバナーが出た日だけ
  下の保存・印刷ボタンが画面外にはみ出して押せなくなる
- iPad注意: Safariは未使用7日でサイトデータを消すことがある→運用は「ホーム画面に追加」必須＋JSONバックアップ
- **個人情報の方針（店主確認済み 2026-08）**: 顧客データはiPad内(IndexedDB)のみ・外部送信なし。
  アプリ原本はGitHub Pagesに置いてよい（個人情報は乗らないため）。タスク7のPWA化で営業中はオフライン動作に
- **バックアップ運用**: 予約データJSONを店のWindows PCの共有フォルダへ（店内Wi-Fi直、ネット非経由）。
  完全自動送信はiPadの制約で不可。Safariの「ダウンロード保存先」を共有フォルダに固定できれば実質2タップ
  →ホーム画面追加アプリで動くかは実機テストで確認、ダメなら保存先を選ぶ数タップ方式に落とす。
  iCloud同期に個人情報が乗らないよう保存先設定に注意。予約入力するiPadは1台に限定（端末間共有なし）。
  - **iOSではホーム画面アプリとSafariでストレージが分かれている可能性が高い**。
    「保存先が選べないときはSafariで開いて書き出す」という逃げ道を店主に案内しかけたが、
    それをやると**予約0件のまま書き出して前のバックアップを空で上書きする**恐れがある。
    手引きからは削除し、代わりに「書き出す前に『保存されている予約：◯件』を確認する」を入れた。
    未検証なので、実機で確かめるまでこの案内を復活させないこと
  PC側の共有フォルダ作成手順書はこちらで用意する（未着手）

## 検証のやり方（Streamlit等は無関係。ヘッドレスChromeで目視相当まで可能）
- 構文: `node --check js/app.js`
- **【重要・2026-09-01】`chrome --screenshot --timeout=...` はもう使えない**。
  Chrome 152 で --timeout が実時間を待たなくなり、IndexedDBの読み込みが終わる前に
  撮影されるようになった（商品も画面遷移も入っていない空の画面が写る）。
  file:// でもHTTPでも同じ。**「アプリが壊れた」ように見えるが壊れていない**ので早合点しないこと。
  → 代わりに **`tools/shot.js`（自作の最小CDPクライアント）** を使う:
  ```
  python -m http.server 8765 --bind 127.0.0.1
  chrome --headless=new --disable-gpu --no-first-run --remote-debugging-port=9222 \
         --user-data-dir=<使い捨て> --window-size=1280,900 about:blank &
  node tools/shot.js "http://127.0.0.1:8765/index.html?nosw=1&demo=1" out.png 4000 "JS式" ...
  ```
  実時間で待ってから撮る／任意のJSを評価できる（`await` 可・戻り値がそのまま出る）ので、
  ボタンを押した結果やIndexedDBの中身まで確認できる。出力先を `.pdf` にすると印刷イメージが出る。
  alert/confirm は評価の最初に `window.alert=()=>{}; window.confirm=()=>true;` で潰せる
  （＝ヘッドレスでconfirm経路も試せるようになった）
- **`?nosw=1` を付けないとService Workerが古いCSS/JSを返す**。
  ローカルHTTPで検証中に「直したのに変わらない」ときはまずこれ。付け忘れて半日溶かした前科あり
- `?nosw=1` を付けても、**使い回したプロファイルは普通のHTTPキャッシュで古いファイルを返す**。
  data/products.js のような更新頻度の低いファイルを直したら、**プロファイルを作り直してから検証する**
  （`SAMPLE_MASTER.products.length` を評価して期待値と合うか先に確かめると早い）
- 画面幅: ヘッドレスは幅500px未満に縮まらない。スマホ幅の検証は500pxで行い実機で最終確認
- URLパラメータ: `?demo=1`=ダミーデータ投入 / `&screen=screen-xxx`=画面直行 /
  `&tap=ボタンID`=自動タップ / `&selftest=1`=デモ予約を保存→一覧表示 /
  `&nosw=1`=Service Workerを登録しない（検証用。古いキャッシュを掴まないため）
- ボタンを追加したら必ず ?tap= で実際にタップ経路を確認（data-goto付け忘れ事故の再発防止）
- **PWA/Service Workerは file:// では一切動かない**。検証はローカルHTTPで:
  `python -m http.server 8765 --bind 127.0.0.1` → `http://127.0.0.1:8765/index.html`
  （localhostは安全なオリジン扱いなのでSWが登録される）
- **ヘッドレスの落とし穴**（2026-08-29に半日溶かした。以下は全部コードのバグではない）:
  - `alert()`を通るとレンダラが異常終了する。スクリーンショットが古いまま/консоль出力も消える。
    → alert前にDBへ書く設計にしてあるので、結果は**IndexedDBを直接ダンプして確認**する
  - `--enable-logging=stderr`のconsole出力は取りこぼす。出ない＝実行されていない、ではない
  - `--dump-dom`は`--timeout`を待たない。IndexedDB系の確認には使えない（画面は「読み込み中…」のまま）
  - SWのCacheStorage書き込みは、**fetchハンドラ側のcache.putをやめたら通るようになった**
    （install と同じURLへの書き込みが競合して "Entry already exists" で全滅していた）。
    設定画面の「アプリの状態」に 14/14 と出れば事前キャッシュは成功している。
    **fetchハンドラでキャッシュに書き戻さないこと**（またハマる）
  - ただし**サーバを止めた状態での起動はヘッドレスでは依然ERR_FAILED**。
    ブラウザを毎回起動し直すためSWが立ち上がる前にナビゲーションが失敗する模様。
    → **オフライン起動の確認は実機（iPadを機内モード）でしかできない**
  - ダウンロード（`a.click()`）はヘッドレスでは保存されずChromeが固まることがある
  - **使い回した--user-data-dirでは?tap=が不発になる**（同じURLでも新規プロファイルなら動く。
    demo=1等の同期処理は動くのにsetTimeout経由のtapだけ落ちる）。**tap検証は毎回新規プロファイルで**
  - `element.focus()` はヘッドレスだと focus/focusin が飛ばないことがある。
    focusinに紐づくUI（キーボードを閉じるボタン等）は `dispatchEvent(new FocusEvent("focusin",{bubbles:true}))`
    で確認する。**ソフトキーボードそのものの挙動はヘッドレスでは再現できない＝実機確認必須**

## 進捗（実装順1-7）
- 済: 1骨組み / 2受注入力 / 3顧客フォーム / 4印刷レイアウト＋2面化 / 5保存・予約一覧・再印刷
- 済: **7 PWA＋JSONバックアップ**（設定画面に「バックアップ」「アプリの状態」を追加）
  - `manifest.webmanifest` / `sw.js` / `icons/`（承の字・小豆色。Pillowで生成）/ `js/backup.js`
  - **sw.jsを直したら必ず CACHE の版番号を上げる**。上げないとiPadに修正が届かない
  - 書き出しは「記録をDBに保存→画面更新→最後にダウンロード発火」の順。
    ダウンロードを踏むと後続JSが中断されることがあるため、順序を変えないこと
  - 「バックアップから戻す」は confirm 3段（読み込むか→入れ替え/追加→全消しの最終確認）。
    分岐は askImportMode() に切り出してNodeで全パターン検証済みだが、
    **confirm/alertを通る経路はヘッドレスで動かせないので、実機で押して確認すること**
  - バックアップ促しバナー: 予約が1件以上あり、未実施 or 7日以上書き出していないと
    ヘッダー下に表示（backupReminderText()は純関数・Nodeでテスト済み）。お客様画面では非表示。
    「閉じる」は次の起動まで
  - 「バックアップから戻す」はボタンを押した瞬間に注意confirm（非常用である旨）→ファイル選択
  - 実機で要確認: ホーム画面追加→機内モードで起動 / 書き出し先にPC共有フォルダを選べるか /
    Safariの「ダウンロード保存先」を共有フォルダに固定できるか /
    読み込みの3段confirmが意図通り出て、1段目キャンセルで何も起きないこと /
    復元入口の注意confirmでキャンセル→ファイル選択が開かないこと・OK→開くこと（confirm直後の
    file input起動がiPadで動くか） / バナーの「閉じる」で消えて再起動で再表示されること
- 済: **商品データ110品**（data/products.js。reference/make_products.py で価格表から生成。
  **手で編集しない**。価格表が変わったら xlsx2json.py → make_products.py の順で作り直す）
- 済: **承り表の新レイアウト**（お客様控＝docxのレシート風・店控＝包材枠つき／
  支払いは済・未に丸／商品内容・菓子包材のプルダウン／支払いトグルは印刷プレビュー画面）
- 済: **6 商品マスタ編集画面**（設定画面内）。**IndexedDBが正**になった:
  - 初回起動で data/products.js を取り込み、以後は店主の編集が残る（js/master.js）
  - 商品の追加/名前・値段・置き場所の変更/かくす/削除、担当者の編集、
    「価格表の内容に戻す」（同梱データで再取り込み。担当者は消さない）
  - 新規商品は id が `u_` 始まり。同梱データ（`p_`）と衝突しない
  - プルダウン候補は商品ごとの sweet / packaging フラグで決まる。
    店主が追加した商品は編集画面のチェックで指定する
  - master.load/save/reseed の挙動は12項目をブラウザ上のテストで確認済み
  - **同梱データに後から足した商品は、稼働中のiPadにも自動で届く**（master.syncBundled・2026-09-01）。
    起動のたびに同梱データと突き合わせ、**まだ届けていないidだけ**をputする。
    既にある商品には触らないので、店で直した値段・名前・かくす設定は消えない。
    届けたidは settings の `seededProductIds` に控え、**店主が消した商品を毎回復活させない**
    - この仕組み以前から使っている端末は、控えが無いので「今ある商品＝届け済み」とみなす。
      よって店主が過去に**削除**した同梱商品が一度だけ復活しうる（かくすなら影響なし）
    - **値段の変更は届かない**。既存idには触らない設計のため。
      値段を配り直すには従来どおり「価格表の内容に戻す」（店の修正は消える）
  - 価格表に無い商品を足すときは **reference/make_products.py の EXTRA** に書いて生成し直す。
    idは `p_x##`（価格表由来の`p_001`とも、店主が足す`u_`とも衝突しない）。
    例: オーダー詰合せ（詰合せ2タブ・price null＝注文のたびにテンキーで金額入力）
- 済: **予約一覧の状態表示（お渡し／お支払い×まだ・済＋確認ダイアログ）**、
  **印刷プレビューの下ボタンを経路で切替**、**版番号表示と「アプリを最新にする」**（2026-09-01）。
  いずれもヘッドレス（tools/shot.js）で確認済み・**実機での確認は未**
- 済: **実機印刷テスト（iPad→AirPrint→Canon TS3730）**（2026-09-01・店主確認）
- 残: 実機で2026-09-01分の確認 — 商品の追加でキーボードが出て保存できるか /
  一覧の「お渡し・お支払い」の確認ダイアログ / 予約一覧から開いたときの下ボタン4つ
- 残: 共有フォルダ運用の実機確認（下記の手引きに沿って店主が設定してみる）

## 店からの現物資料（reference/・**gitignore対象**）
店名＝**菓子舗 丸円堂**（まるえんどう。当初「丸の堂」と誤読していた）。2026-08-29受領。
GitHub Pagesは公開なので当面コミットしない。
- `丸円堂価格表_最新.xlsx`＝**価格の正本**（110品・詰合せは「中身」列つき）。実装は
  `価格表_最新.json` を読む。旧版79品からは純粋な追加のみで価格変更なし（`_2026-08`は差分用に残置）
- 単価手入力(price:null)は15件。価格欄が「━」＝未定の行あり（変換スクリプトで対応済み）
- `photos/` 26枚＝紙の承り表の記入見本＋手書き指示、レジ（TERAOKA）全カテゴリ画面、
  詰合せ商品の物撮り11枚（アプリの商品写真に使える）
- **レジ写真は旧価格が混じる**。価格が食い違ったらExcelを採る
- Excel8分類→レジ5タブへの寄せ方は reference/make_products.py の TAB/TSUME2 を参照
  （箱、籠詰合の12品のうち詰合せ7〜10だけ「詰合せ2」タブ、残りは「詰合せ1」タブ）
- その他の要確認事項（品名の揺れ・レジのみ/価格表のみの商品）は reference/README.md に一覧
- 新しい価格表が届いたら:
  `python reference/xlsx2json.py <新xlsx> reference/価格表_YYYY-MM.json` でJSON化（動作確認済み）。
  ふりがな除外・品名の空欄引き継ぎ込み。**コンソールの文字化けは端末の表示だけでJSONは正常**
